<%= render "shared/page_container" do %>
  <div class="max-w-2xl w-full mx-auto p-8 bg-white rounded-xl shadow-lg">
    <div class="text-center mb-6">
      <h2 class="text-3xl font-extrabold text-gray-900">New Feeding</h2>
      <p class="mt-2 text-sm text-gray-600">Record <%= @child.first_name %>'s feeding</p>
    </div>

    <%= render "form", feeding: @feeding %>
  </div>
<% end %> 
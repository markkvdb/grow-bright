<%= render "shared/page_container" do %>
  <div class="max-w-md w-full mx-auto space-y-8 p-8 bg-white rounded-xl shadow-lg">
    <div class="text-center">
      <h2 class="mt-4 text-3xl font-extrabold text-gray-900">Edit <%= @child.first_name %></h2>
      <p class="mt-2 text-sm text-gray-600">Update your child's information</p>
    </div>

    <%= render "form", child: @child %>
  </div>
<% end %> 
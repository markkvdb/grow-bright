<%= render "shared/page_container" do %>
  <div class="max-w-2xl mx-auto p-8 bg-white rounded-xl shadow-lg">
    <div class="text-center mb-6">
      <h2 class="text-3xl font-extrabold text-gray-900">New Measurement</h2>
      <p class="mt-2 text-sm text-gray-600">Record <%= @child.first_name %>'s measurements</p>
    </div>

    <%= render "form", measurement: @measurement %>
  </div>
<% end %> 